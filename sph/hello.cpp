#include <algebra/Algebra_kernel.h>
#include <iostream>
using namespace std;

int main()
{
    // Matrix A = { { 1, 1, 1 }, { 2, -1, -1 }, { 2, -4, 10 } };
    Matrix A = { { 1, 1 }, { 2, -1 }, { 2, -4 } };
    cout << A << endl;

    return 0;
}
